// Copy this file to `config.local.js` and replace the placeholder with your real key.
// Make sure `config.local.js` is listed in .gitignore so you don't commit secrets.

// Example:
// window.API_KEY = 'your_real_api_key_here';

// Leave commented in the example to avoid accidental use.

// window.API_KEY = 'REPLACE_ME_WITH_YOUR_KEY';
